# search/ — Phase 4 placeholder

This directory is reserved for **Phase 4** of quant-lab (see top-level README for the full phased roadmap).

**Status: scaffolded only.** DB tables exist (see `sql/001_init.sql`) but no code yet.

When implemented, this module will:
- Announcement / news corpus collection
- pg_trgm + zhparser full-text search index
- REST endpoints /search?q=... (filter by symbol, date range)
