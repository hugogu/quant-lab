# backtest/ — Phase 3 placeholder

This directory is reserved for **Phase 3** of quant-lab (see top-level README for the full phased roadmap).

**Status: scaffolded only.** DB tables exist (see `sql/001_init.sql`) but no code yet.

When implemented, this module will:
- vectorbt-based strategy backtest
- Walk-forward validation
- Compare strategy vs CSI 300 buy-and-hold baseline
- Export reports to backtest_reports/ table
